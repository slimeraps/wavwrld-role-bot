# Copy this file to .env for local testing (or set as secrets on your host).
# The bot will refuse to start without DISCORD_TOKEN.

DISCORD_TOKEN=your-bot-token-here

# Optional. Where roles.json and the persisted config.json live.
# Defaults to the project folder when running locally.
# DATA_DIR=/data
